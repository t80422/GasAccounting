---
description: 
globs: 
alwaysApply: true
---
### **WinForms 專案規範 (使用 MVP 模式、Repository 模式和 Entity Framework)**

#### **1. 程式碼結構**
- **分層架構**：
  - **View 層**：負責使用者介面，僅處理顯示和事件觸發，不包含業務邏輯。
  - **Presenter 層**：負責處理 View 的事件，並與 Model 層互動，實現業務邏輯。
  - **Model 層**：包含資料模型、業務邏輯和資料存取邏輯，使用 Repository 模式和 Entity Framework 進行資料庫操作。

- **資料夾結構**：
  - `Views`：存放所有 WinForms 表單和 UI 相關檔案。
  - `Presenters`：存放 Presenter 類別，負責處理 View 的事件和業務邏輯。
  - `Models`：存放資料模型、業務邏輯和 Repository 類別。
  - `Repositories`：存放 Repository 類別，負責與資料庫的互動。
  - `Services`（可選）：如果需要進一步分離業務邏輯，可以新增此資料夾。

#### **2. MVP 模式的最佳實踐**
- **View 層**：
  - 僅負責顯示資料和觸發事件，不包含任何業務邏輯。
  - 透過介面 (`IView`) 與 Presenter 層進行溝通，確保 View 和 Presenter 的解耦。
  - 範例：
    ```vb
    Public Interface IMyView
        Property Data As String
        Event SaveClicked As EventHandler
    End Interface
    ```

- **Presenter 層**：
  - 負責處理 View 的事件，並與 Model 層互動。
  - 不直接操作資料庫，所有資料存取都透過 Model 層進行。
  - 範例：
    ```vb
    Public Class MyPresenter
        Private _view As IMyView
        Private _model As IMyModel

        Public Sub New(view As IMyView, model As IMyModel)
            _view = view
            _model = model
            AddHandler _view.SaveClicked, AddressOf OnSaveClicked
        End Sub

        Private Sub OnSaveClicked(sender As Object, e As EventArgs)
            _model.SaveData(_view.Data)
        End Sub
    End Class
    ```

- **Model 層**：
  - 包含業務邏輯和資料存取邏輯。
  - 使用 Repository 模式封裝資料庫操作，並透過 Entity Framework 實現。
  - 範例：
    ```vb
    Public Interface IMyModel
        Sub SaveData(data As String)
    End Interface

    Public Class MyModel : Implements IMyModel
        Private _repository As IMyRepository

        Public Sub New(repository As IMyRepository)
            _repository = repository
        End Sub

        Public Sub SaveData(data As String) Implements IMyModel.SaveData
            _repository.Save(data)
        End Sub
    End Class
    ```

- **Repository 層**：
  - 負責與資料庫的互動，使用 Entity Framework 進行 CRUD 操作。
  - 每個 Repository 類別應實作對應的介面，以便於測試和替換。
  - 範例：
    ```vb
    Public Interface IMyRepository
        Sub Save(data As String)
    End Interface

    Public Class MyRepository : Implements IMyRepository
        Private _context As MyDbContext

        Public Sub New(context As MyDbContext)
            _context = context
        End Sub

        Public Sub Save(data As String) Implements IMyRepository.Save
            ' 使用 Entity Framework 進行資料庫操作
            Dim entity = New MyEntity With {.Data = data}
            _context.MyEntities.Add(entity)
            _context.SaveChanges()
        End Sub
    End Class
    ```

#### **3. 規則**
- **UI 程式碼修改規則**：
  - 不允許直接修改 UI 層的程式碼，所有建議的修改內容將在聊天視窗中顯示。
  - 如果需要修改 UI，請先與團隊討論並確認修改的必要性。

- **資料庫操作規則**：
  - 所有資料庫操作都必須透過 Repository 層進行，禁止在 Presenter 或 Model 層直接使用 DbContext。
  - 確保每個 Repository 類別都實作對應的介面，以便於測試和替換。

- **業務邏輯規則**：
  - 業務邏輯應集中在 Model 層，避免在 Presenter 或 View 層實現業務邏輯。
  - 確保業務邏輯的獨立性，避免與 UI 或資料庫操作耦合。

- **測試規則**：
  - 每個 Presenter、Model 和 Repository 類別都應有對應的單元測試。
  - 使用 Mock 物件進行測試，確保測試的獨立性和可靠性。

#### **4. 其他注意事項**
- **命名規範**：
  - 類別和方法名稱應使用 PascalCase。
  - 變數名稱應使用 camelCase。
  - 介面名稱應以 `I` 開頭，例如 `IView`、`IPresenter`、`IModel`。

- **錯誤處理**：
  - 使用 try-catch 區塊處理可能發生的例外，並記錄錯誤訊息。
  - 在 View 層顯示友好的錯誤訊息，避免直接顯示技術細節。

- **效能優化**：
  - 避免在迴圈中進行資料庫查詢，盡量使用批量操作。
  - 使用 Entity Framework 的 `AsNoTracking` 方法來提高查詢效能。

---

這份規範已根據 MVP 模式進行調整，並確保與 Repository 模式和 Entity Framework 的整合。如果有任何需要進一步修改或補充的地方，請隨時告知！
